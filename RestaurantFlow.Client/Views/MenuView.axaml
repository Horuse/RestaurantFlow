<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:RestaurantFlow.Client.ViewModels"
             xmlns:shadui="clr-namespace:ShadUI;assembly=ShadUI"
             xmlns:asyncImageLoader="clr-namespace:AsyncImageLoader;assembly=AsyncImageLoader.Avalonia"
             mc:Ignorable="d" d:DesignWidth="1200" d:DesignHeight="800"
             x:Class="RestaurantFlow.Client.Views.MenuView"
             x:DataType="vm:MainWindowViewModel">

    <Design.DataContext>
        <vm:MainWindowViewModel/>
    </Design.DataContext>

    <ScrollViewer>
        <ItemsControl ItemsSource="{Binding FilteredMenuItems}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="4"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <shadui:Card
                        HasShadow="True"
                        Margin="10"
                        >
                        <shadui:Card.Header>
                            <StackPanel Spacing="8">
                                <Border 
                                        Height="150" 
                                        CornerRadius="8"
                                        HorizontalAlignment="Stretch"
                                        ClipToBounds="True">
                                    <Panel>
                                        <Image asyncImageLoader:ImageLoader.Source="{Binding ImageUrl}"
                                               IsVisible="{Binding HasImage}"
                                               HorizontalAlignment="Center" 
                                               VerticalAlignment="Center">
                                        </Image>
                                        
                                        <TextBlock Text="🍔" 
                                                   FontSize="60" 
                                                   HorizontalAlignment="Center" 
                                                   VerticalAlignment="Center"
                                                   IsVisible="{Binding !HasImage}"/>
                                        
                                        <StackPanel Orientation="Horizontal" 
                                                    HorizontalAlignment="Right" 
                                                    VerticalAlignment="Top" 
                                                    Margin="8" 
                                                    Spacing="4">
                                            <Border Background="#FF4444" 
                                                    CornerRadius="12" 
                                                    Padding="6,4"
                                                    IsVisible="{Binding IsPopular}">
                                                <TextBlock Text="Популярне" 
                                                          Foreground="White" 
                                                          FontSize="10" 
                                                          FontWeight="Bold"/>
                                            </Border>
                                            <Border Background="#4CAF50" 
                                                    CornerRadius="12" 
                                                    Padding="6,4"
                                                    IsVisible="{Binding IsRecommended}">
                                                <TextBlock Text="Рекомендуємо" 
                                                          Foreground="White" 
                                                          FontSize="10" 
                                                          FontWeight="Bold"/>
                                            </Border>
                                        </StackPanel>
                                    </Panel>
                                </Border>
                                
                                <shadui:CardTitle HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding Name}"/>
                                </shadui:CardTitle>
                                <shadui:CardDescription HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding Description}" TextWrapping="Wrap"/>
                                </shadui:CardDescription>
                                
                               
                                <StackPanel Spacing="4" Margin="8,0">
                           
                                    <TextBlock Text="{Binding Ingredients, StringFormat='Інгредієнти: {0}'}" 
                                              FontSize="11" 
                                              Foreground="#666"
                                              TextWrapping="Wrap"
                                              IsVisible="{Binding HasIngredients}"/>
                                    
                                    <TextBlock Text="{Binding Allergens, StringFormat='Алергени: {0}'}" 
                                              FontSize="11" 
                                              Foreground="#FF6600"
                                              TextWrapping="Wrap"
                                              IsVisible="{Binding HasAllergens}"/>
                                    
                                    <StackPanel Orientation="Horizontal" 
                                                HorizontalAlignment="Center" 
                                                Spacing="15">
                                        <StackPanel Orientation="Horizontal" Spacing="4"
                                                   IsVisible="{Binding HasCalories}">
                                            <TextBlock Text="{Binding FormattedCalories}" 
                                                      FontSize="11" 
                                                      FontWeight="Medium"
                                                      Foreground="#666"/>
                                        </StackPanel>
                                        
                                        <StackPanel Orientation="Horizontal" Spacing="4"
                                                   IsVisible="{Binding HasCookingTime}">
                                            <TextBlock Text="{Binding FormattedCookingTime}" 
                                                      FontSize="11" 
                                                      FontWeight="Medium"
                                                      Foreground="#666"/>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </shadui:Card.Header>
                        
                        <shadui:Card.Footer>
                            <DockPanel>
                                <TextBlock 
                                    Text="{Binding Price, StringFormat='₴{0:F2}'}" 
                                    FontSize="18" 
                                    FontWeight="Bold" 
                                    Foreground="#DA291C"
                                    VerticalAlignment="Center"
                                    HorizontalAlignment="Left"/>
                                
                                <Button 
                                    Classes="Primary"
                                    Content="Додати"
                                    HorizontalAlignment="Right"
                                    Command="{Binding $parent[UserControl].((vm:MainWindowViewModel)DataContext).AddToCartCommand}"
                                    CommandParameter="{Binding}"/>
                            </DockPanel>
                        </shadui:Card.Footer>
                    </shadui:Card>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</UserControl>